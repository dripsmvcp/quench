global:
  scrape_interval: 5s
  scrape_timeout: 4s
  evaluation_interval: 15s

scrape_configs:
  - job_name: quench
    metrics_path: /metrics
    static_configs:
      - targets:
          - quench-server:8080
        labels:
          service: quench
